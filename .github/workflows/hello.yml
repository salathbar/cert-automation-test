name: Hello World Test

on:
  workflow_dispatch:   # allows manual trigger
  push:                # runs on every push

jobs:
  hello:
    runs-on: self-hosted
    steps:
      - name: Say hello
        run: echo "$(date '+%Y-%m-%d %H:%M:%S')👋 Hello from GitHub Actions!" | tee -a /tmp/gha-job.log
